// ChatInterface — query input + answer display + source chunks panel
// POST to /query with {query, conversation_id}.
// Displays answer and intent badge (Fact/Summary/Comparison/Conversational).
// Renders SourceChunksPanel below answer.
import SourceChunksPanel from './SourceChunksPanel'

export default function ChatInterface({ conversationId }) {
  return null // TODO: implement
}
